-- CREATE TABLE IF NOT EXISTS users
-- (
--     id        BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, -- id is auto-incremented and used as the primary key
--     email     VARCHAR(255),                                        -- email field, nullable
--     firstname VARCHAR(255),                                        -- firstname field, nullable
--     lastname  VARCHAR(255),                                        -- lastname field, nullable
--     telephone VARCHAR(255),                                        -- telephone field, nullable
--     role      VARCHAR(255) DEFAULT 'USER',                         -- role is stored as a string, with a default value of 'USER'
--     password  VARCHAR(255)                                         -- password field, nullable
-- );
-- --
-- CREATE TABLE IF NOT EXISTS cash_card
-- (
--     id      BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, -- id is auto-incremented and used as the primary key
--     amount  DOUBLE PRECISION, -- amount field, nullable
--     owner   VARCHAR(255),     -- owner field, nullable (can store the owner's name)
--     user_id BIGINT NOT NULL,  -- Foreign key linking to the users table, not nullable
--     CONSTRAINT fk_user
--         FOREIGN KEY (user_id)
--             REFERENCES users (id)
--             ON DELETE CASCADE -- Delete all cash cards when the user is deleted
-- );
-- --
-- -- -- Indexes for performance optimization
-- -- -- Optional: Drop the existing index if it exists
-- -- DROP INDEX IF EXISTS idx_user_email;
-- --
-- -- -- Create the index
-- -- CREATE INDEX IF NOT EXISTS idx_user_email ON users (email);
-- --
-- -- -- Optional: Drop the existing index if it exists
-- -- DROP INDEX IF EXISTS idx_cashcard_user_id;
-- --
-- -- -- Create the index
-- -- CREATE INDEX IF NOT EXISTS idx_cashcard_user_id ON cash_card (user_id);
-- --
